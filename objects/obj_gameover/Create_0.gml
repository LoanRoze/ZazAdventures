// Sprites déjà créés dans le projet
spr_title   = spr_gameover;      // Sprite du titre Game Over
spr_restart = spr_btn_restart;   // Sprite du bouton Restart
spr_leave   = spr_btn_leave;     // Sprite du bouton Leave

// Offsets verticaux pour placement
title_offset   = -150; // au-dessus du centre
restart_offset = 0;    // centré verticalement
leave_offset   = 80;   // sous le bouton restart